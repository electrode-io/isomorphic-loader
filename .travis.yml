language: node_js
node_js:
  - v8
  - v10
before_install: npm install -g fyn
install: eval `fyn bash` && fyn --pg none install
script: eval `fyn bash` && npm run coverage
notifications:
  email:
    - joel123@gmail.com
